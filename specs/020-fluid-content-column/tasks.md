# 任务：流体内容列

**规格**：[spec.md](./spec.md) ｜ **计划**：[plan.md](./plan.md) ｜ **调研**：[research.md](./research.md)

这一轮的代码改动是**四个文件、约五行**。任务多不是因为改得多，
而是因为**验收比改动难** —— 核心断言写不进 vitest，得在真实浏览器里量，
而且必须证明它会红。

---

## 第 1 阶段：先立验收（在动第一行代码之前）

**这一阶段必须先做完。** 改完再写脚本，脚本会不自觉地按现状写成永远为真 ——
018 的概览页测试就是这么来的（断言 `grid-cols-[...]`，版面正确时反而报红）。

- [X] T001 写 `specs/020-fluid-content-column/measure.mjs`：量三档流动窗口的留白占比
      与两档地板窗口的内容列宽度，不通过则退出码 1
- [X] T002 **改代码之前跑一遍，确认它现在报错**。读数存进 `quickstart.md`
      —— 改造前的数字事后补不回来
- [ ] T003 起本地实例、确认十六个路由都打得开（`quickstart.md` 顶部命令）

## 第 2 阶段：US2 窄屏零回归的基线（P1，阻塞验收）

- [ ] T004 [US2] 在 **1440 / 1366 / 1280** 三档各拍一轮十六个路由的截图，
      存 `.playwright-mcp/before-020/`。**这是 SC-002 唯一的判据** ——
      「窄屏没变」只能靠前后对比证明

## 第 3 阶段：US1 + US3 换规则（P1/P2）

四个文件，逐处改，**不用 `sed` 批量替换**（风险表第一条）。

- [ ] T005 [US1] `web/src/routes/AppShell.tsx`：内容列 `max-w-[960px]` 换成
      `w-[min(100%,max(960px,76%))]`。**不要保留 `max-width`** ——
      任何上限都会在超过它之后重新产生 FR-002 禁止的行为（FR-001/002/003/004）
- [ ] T006 [P] [US3] `web/src/routes/Import.tsx`：删掉 `max-w-[760px]`（FR-006）
- [ ] T007 [P] [US3] `web/src/features/transfers/TransferForm.tsx`：删掉 `max-w-[640px]`（FR-006）
- [ ] T008 [US3] **`web/src/features/common/ListToolbar.tsx` 的 `max-w-[640px]` 保持不变。**
      本任务只是把这个判断写进那一行的注释：**它约束的是控件宽度，不是内容列**。
      它和 T007 删掉的是同一个数字，下一个人做批量替换时会连它一起改，
      而**不会有任何测试报错**（FR-007）

## 第 4 阶段：验收（P1）

- [ ] T009 跑 `node specs/020-fluid-content-column/measure.mjs` —— **必须通过**，
      三档留白占比极差 ≤5 个百分点，地板档内容列 960。
      **这一步同时反证了 FR-005**：若按视口而非面板算比例，占比就不会恒定（SC-001 / FR-004 / FR-005）
- [ ] T010 **双向验证**：把 T005 改回 `max-w-[960px]`，重跑脚本 —— **必须报错**；
      再改回来 —— **必须通过**。**这一步不能省** ——
      一条只见过它通过的版面断言不构成任何证据（SC-005）
- [ ] T011 [US2] 1440 / 1366 / 1280 三档重拍十六个路由，与 T004 的基线**逐张比对**。
      **任何一处不同都是回归**（SC-002）
- [ ] T012 [US3] 静态检查：扫 `routes/` 与 `features/` 下**所有** `max-w-`（排除 `sm:max-w`，
      那是对话框），**命中集合必须等于白名单**：

      | 允许 | 它在决定什么 |
      |---|---|
      | `routes/AppShell.tsx` | 内容列 —— 唯一一处页面级宽度 |
      | `features/common/ListToolbar.tsx` | 搜索框，是控件 |
      | `routes/Assets.tsx` 的 `max-w-48` | 备注列的截断宽度，是单元格 |

      **不要只枚举 960/760/640 三个字面量** —— 那证明不了 SC-003，
      以后有人写 `max-w-4xl` 当内容列，枚举式的检查看不见（SC-003 / FR-007）
- [ ] T013 新增一条 DOM 测试守 T012 那个**静态事实**（宽度声明只有一处）。
      它测的是代码结构不是布局，所以 jsdom 能测。
      **章程原则二要求触及 UI 必须有 DOM 测试**，而这一轮唯一能被 jsdom 断言的就是这条
- [ ] T014 跑 `npx vitest run --maxWorkers=4` —— **预期 371 全绿加新增的**。
      有红说明动到了 DOM 结构，停下来查

## 第 5 阶段：文档同步（P2）

**这一阶段和代码一样重要**：留下与代码相反的数字，下一个人会以为代码写错了。

- [ ] T015 [P] `docs/rules/web-tables.md` 的版面数字表：`内容列 ≤960px 贴左`
      改成新规则，并写明**地板 960、无上限、按面板比例**（FR-011 / SC-006）
- [ ] T016 [P] `specs/018-organic-layout/quickstart.md` 第 1 步的验收标准：
      「宽度 ≤960px」不再成立。**018 的 spec 本身不改** ——
      它记录的是当时的真实决定；改的是走查步骤这种会被照着执行的东西
- [ ] T017 [P] `docs/design/organic-layout/README.md`：记下**第三处**「稿子正文与它自己的产物不一致」
      —— 960 这个数字只在 ~1440 下被验证过，九张参考屏全部按这一档画。
      **`handoff.md` 本身不编辑**，与前两处处理方式一致（FR-012）

## 第 6 阶段：收口

- [ ] T018 跑完整门禁（章程七条）：`gofmt -l` 空、`go vet`、`golangci-lint` 零告警、
      `go test ./...` 全过、`nexus verify`、`npx tsc --noEmit`、`eslint`、
      `vitest run --maxWorkers=4`、`npm run build`、真镜像 `deploy/smoke.sh`
- [ ] T019 **验证 SC-004**：`git diff main --stat -- internal/ cmd/ migrations/` 必须为**空**
- [ ] T020 按 [quickstart.md](./quickstart.md) 走完 7 步，带尺子量。
      **第 5 步覆盖 FR-009**（字号行高圆角未随窗口变化），**第 7 步覆盖 SC-006**（文档无相反数字）
- [ ] T021 **前后对比**：把 2560 下的改造前后截图并排给开发者。
      「27 寸上的不平衡修好了没有」只有这样才回答得了

---

## 依赖与并行

```
第 1 阶段 先立验收（T001–T003）
        ↓
第 2 阶段 窄屏基线（T004）      ← 必须在改代码之前
        ↓
第 3 阶段 换规则（T005–T008）   T006/T007/T008 可并行
        ↓
第 4 阶段 验收（T009–T014）     T010 的双向验证不可省
        ↓
第 5 阶段 文档（T015–T017）     三条可并行
        ↓
第 6 阶段 收口（T018–T021）
```

**第 1、2 阶段必须在第 3 阶段之前**：改造前的读数与截图事后补不回来。

**任务总数 21，其中 5 个标了 `[P]`。没有需要开发者裁定的确认点** ——
六条裁定在 `/grill-me` 里已经逐条决断完毕。

**MVP = 第 1–4 阶段**（T001–T014）：规则换完并证明验收会红会绿。
文档与收口不可省，但它们不改变产品行为。
