# 任务清单：内容列铺满面板

**分支**：`021-content-column-fills-panel` ｜ **规格**：[spec.md](./spec.md) ｜ **计划**：[plan.md](./plan.md)

> **本轮是追认。** 实施类任务在补规格**之前**就已完成（提交 `6ca7139`），
> 因此标注为 `[X]` 并附证据。**只有真正跑过的才打勾** ——
> 未执行的走查项留空，它们是本轮剩下的实际工作，不是形式。

**MVP 范围**：US1 + US2（宽屏没有空白、右边缘收得住）。US3、US4 是同一次改动的守门人。

---

## 第 1 阶段：Setup

- [X] T001 确认走查实例跑的是**当前**构建 —— `npm --prefix web run build` 后再
  `go build`，二进制里 embed 的是 `web/dist`
  证据：本轮踩过一次，8818 上是旧二进制 `/tmp/nexus018`；新构建另起 `:8819`，
  入口 JS 由 `index-DpMcNZu5.js` 变为 `index-CHeVbcar.js` 后才开始验收

---

## 第 2 阶段：Foundational（阻塞所有用户故事）

**验收必须先立，且必须在改代码之前跑一遍拿到「改造前」读数。**
改完再写脚本，脚本会不自觉地按现状写成永远为真。

- [X] T002 改写 `specs/020-fluid-content-column/measure.mjs` 的断言：
  留白 ≤1px、右缘距屏幕 52±1px，替换 020 的「占比恒 24%、地板恰为 960」
  证据：脚本头部记录了断言变更的日期与原因；读数见 research 问题四
- [X] T002a analyze 补缺口：桌面加 1280 档，另加移动两档（700/390）断言 `edge=20`
  证据：八档全过；FR-002a / SC-002a 因此成立
- [X] T003 脚本住址不分叉 —— 复用 020 目录下的同一个探针，不在 021 目录另建一份
  证据：`specs/021-*/` 下无 `measure.mjs`；plan.md「项目结构」记录了理由

---

## 第 3 阶段：US1 — 宽屏上没有那片空白（P1）

**独立测试**：2560 下打开概览与资产页，内容列右缘到屏幕为 52px 而非 581px。

- [X] T004 三变体对比出图：同一构建、同一数据、同一页面，只用 CSS 覆盖切换宽度规则，
  2560 / 1920 / 1440 各出 A/B/C 三张
  证据：research 问题一的实测表；开发者据此裁定 B
- [X] T005 删掉 `web/src/routes/AppShell.tsx` 内容列 `div` 上的
  `w-[min(100%,max(960px,76%))]`，**不换成别的类**
  证据：`6ca7139`；该 `div` 现在没有 `className`
- [X] T006 重写 `AppShell` 该处注释，记录三次修订与本轮的代价
  证据：注释含 018/020/021 三条规则的对照与「宽表格列散开、960 地板消失」两项代价
- [X] T007 跑实测 —— 留白必须为 0、桌面档右缘必须为 52（FR-005 的「不封顶」由 3840 档承担）
  证据：桌面六档 3484/2204/1564/1084/1010/924 全部 `void=0`、`edge=52`，退出码 0

---

## 第 4 阶段：US2 — 右边缘留得住（P1）

**独立测试**：2560 下右内边距为 40px，与左侧 56px 读作同一套收边。

- [X] T008 量 C 变体（右内边距压到井槽 12px）并出图，判断能否再抢 28px
  证据：research 问题二 —— 左 56 / 右 12 读作没对齐且局促。
  **注**：该问题最初写的「按钮与 28px 弧线相交」已被 T010 实测推翻并改写
- [X] T009 保留 `pr-10`（40px），恒定右缘 = 40 + 12 = 52px，**不引入新常量**
  证据：FR-002 / FR-003；52 是两个既有值相加
- [X] T010 2560 实机量右上角按钮到面板右缘/上缘的距离（quickstart 第 3 步）
  证据：`Add device` 右 40 / 上 46，`Export CSV` 右 159 / 上 45，圆角半径 28 ——
  **两个方向都清开圆角方框**。这条实测**推翻了 research 问题二最初的理由**，
  已连同 spec FR-003、plan、web-tables、AppShell 注释一并改写

---

## 第 5 阶段：US3 — 窄屏只变宽度（P1）

**独立测试**：1440 / 1366 / 1280 逐路由对比 020 截图 —— 宽度不同，其余逐像素相同。

- [X] T011 确认 960 地板一并去掉，并写明代价
  证据：research 问题三；1440 下 960 → 1084
- [X] T012 1440 / 1366 / 1280 三档打开**全部 11 个路由**，逐项比对不变量
  证据：`h1` 字号/行高/字族、正文字号行高、单元格内边距与字号、表格行高、
  按钮高度与圆角，三档之间**全部一致**；内容列 1084/1010/924 是唯一随宽度变的量；
  11×3 处均无横向滚动条。比对的是计算样式而不是眼睛看截图
- [X] T013 1280 档补量并写进脚本（不再靠人记得跑）
  证据：1280 → 包含块 924、内容列 924、留白 0、右缘 52
- [X] T014 横向溢出检查：`documentElement` 与 `main` 均不横向滚动（SC-007）
  证据：2560 下实测 `{bodyScrolls: false, mainScrolls: false}`

---

## 第 6 阶段：US4 — 规则只有一处，而且已经没有数字了（P2）

**独立测试**：`max-w-` 命中集合等于白名单。

- [X] T015 更新 `web/tests/contentColumn.test.ts` 的文档注释：说明 `AppShell` 里
  已无数字可与 `max-w-` 抵消，这条测试升为**唯一拦截**（断言未变）
  证据：`6ca7139`；NFR-001
- [X] T016 跑 quickstart 第 4 步的 grep
  证据：命中恰为 `Assets.tsx` 的 `max-w-48`（备注列截断）与 `ListToolbar.tsx` 的
  `max-w-[640px]`（搜索框），与白名单一致；`AppShell` 内容列 `div` 无 `className`

---

## 第 7 阶段：Polish & 跨领域

- [X] T017 **双向验证**：贴回 020 的规则 → 全红（桌面五档 836/529/375/124/50px）；
  去掉 → 全过（SC-003）
  证据：research 问题四的反向读数表；两个方向都实际跑过
- [X] T018 全量前端测试 `--maxWorkers=4`
  证据：39 个文件 / 375 例全过
- [X] T019 服务端零改动，对 `main` 复核（不只是工作区）
  证据：`git diff --stat main -- internal/ cmd/` 输出为空 —— **跨全部 50 个提交**为零
- [X] T019a FR-010 复核，基线是本轮而非 `main`
  证据：`6ca7139` 未碰 i18n，其后至工作区也未碰。对 `main` 的 `zh.ts` 36 行差异
  属于 017/018/020，**用 `main` 当基线会让这条检查假红**，走查里已写明
- [X] T020 `docs/rules/web-tables.md` 内容列一节重写 + 三条规则实测对照表（FR-011）
- [X] T021 `docs/design/organic-layout/README.md` 记第四处「稿子与现实不符」，
  重点写「指标达成而目标没达成」这条教训
- [X] T022 020 的 `spec.md` 加被取代横幅，**正文一字不改**（FR-012）
- [X] T023 020 的 `quickstart.md` 改写双向验证与窄屏走查两步
- [X] T024 `CLAUDE.md` 决策编号 → 1–135，范围 → `specs/021-*`，
  当前计划指针 → 021
- [X] T025 全仓 grep `76%` 与 `min(100%,max(960px`，只剩历史对照表（SC-006）
- [X] T026 `handoff.md` **不编辑** —— 与前三处不一致的处理方式一致（FR-012）
- [X] T027 一次提交本轮的规格与文档，**不推送**

---

## 依赖关系

```
T001（构建正确）── 阻塞所有实测
T002/T003（验收先立）── 阻塞 T005（改代码）
T005 ── 阻塞 T007、T017
T017（双向验证）── 阻塞 T027（可提交）
```

US1 与 US2 是同一次改动的两面，不可分割实施。
US3、US4 是守门人，可在 US1/US2 之后独立验证。

## 并行机会

- T010 / T012 / T013 / T016 / T019 互不依赖，可同时进行（本轮剩余工作）
- T020–T026 的文档改动互不依赖（已完成）

## 剩余工作

**只剩一项**：T027 提交（不推送）。

analyze 阶段查出七条问题，都已在 implement 阶段处理完：

| ID | 问题 | 处理 |
|---|---|---|
| C1 | **ESLint 报错，门禁 2 不通过** —— `web/.ds-types` 等四个 design-sync 生成目录已 gitignore 但不在 eslint 的 `ignores` 里 | `eslint.config.js` 加四条；lint 与 `tsc --noEmit` 均零错 |
| A1 | **FR-002 自相矛盾** —— 「右缘恒 52px」在 `md` 以下是假的（实测 20px），而规格自己的 Edge Cases 承认换了内边距 | 拆成 FR-002 / FR-002a，SC-002a 补验收，脚本加移动两档 |
| A2 | SC-005 后半句在追认轮次里空转（本轮未改任何断言） | 改写为「全量通过 + 红绿证据由 SC-003 承担」 |
| A3 | 走查要 1280 而脚本不量 | 1280 进 `WIDTHS`，T013 从此自动化 |
| A4 | FR-010 无任务无 SC | 补进走查第 5 步，且**基线是 `6ca7139` 不是 `main`** |
| A5 | FR-005 无专属任务 | 由 T007 的 3840 档承担，已在任务里点名 |
| A6 | 移动端只在 Edge Cases 出现、无验收 | 随 A1 一并补 SC-002a |

另有一条不是 analyze 查出、而是 T010 实测查出的：

| | 问题 | 处理 |
|---|---|---|
| **T010** | **「12px 会让内容切进 28px 圆角」是错的** —— 按钮距面板上缘 46px，圆角方框只占 28px，两种内边距下都进不去。这个理由我从截图上读出来，写进了五个文件 | spec FR-003、research 问题二、plan、web-tables、AppShell 注释、quickstart 第 3 步全部改写为真实理由（左右差与局促感）。research 里保留了这次推翻的记录 |

**本轮两次「看起来对、量出来不对」**：020 的留白占比是第一次，圆角是第二次。
