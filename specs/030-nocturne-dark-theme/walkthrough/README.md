# 走查存档

中文界面逐屏、逐弹窗的 Playwright 截图，与 `../design/prototype.dc.html` 对应屏并排。
命名：`NN-<screen>.png` / `dNN-<dialog>.png`。差异逐条记在 `../quickstart.md`。

## 原型对照

同名加 `.proto.png` 的是原型在同一视口（1440×900）下的同一屏 / 同一弹窗，
由 `design/prototype.dc.html`（需要 `support.js` 与 `_ds/*/_ds_bundle.js` 在位）用它自己的导航打开后截取。
`01-login.proto.png` 从原型的「退出登录」进入。两张并排看，差异表在 `quickstart.md`。
