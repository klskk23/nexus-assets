package httpapi

// User-facing text used to live here as a block of Chinese constants.
//
// It moved to internal/i18n, where each string has a key and a translation in
// every supported language. Keeping a second copy in this package would put
// the two out of step the first time either was edited -- which is the failure
// the catalogue exists to prevent -- so this file deliberately holds none.
//
// Constitution principle V still applies: identifiers, comments, logs and
// error codes are English; anything a person reads on screen goes through the
// catalogue.
