# Go
*.exe
*.test
*.out
/nexus
/nexus-*
vendor/

# SQLite
*.db
*.db-wal
*.db-shm

# Node / frontend
node_modules/
*.tsbuildinfo
web/dist/*
!web/dist/.gitkeep
*.log

# Env — the real file holds the JWT secret and the bootstrap password;
# the example is committed so a new checkout knows what to fill in.
.env
.env.*
!.env.example

# Editors / OS
.DS_Store
Thumbs.db
*.swp
*.tmp
.idea/
.vscode/

# Coverage
coverage/
c.out

# design-sync 的暂存脚本与构建产物（017 之后同步设计系统时用）
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
# 转换器读的自足样式表（由 buildCmd 从 dist/ 生成）
web/.ds-css/

# The local walkthrough instance's screenshots and its restart script.
.playwright-mcp/
web/ds-src/
web/.ds-types/
web/index.d.ts
